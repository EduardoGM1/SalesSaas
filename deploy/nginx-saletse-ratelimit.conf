# http{} — zonas de rate limit Salètse (no van dentro de server{})
limit_req_zone $binary_remote_addr zone=saletse_login:10m rate=10r/m;
limit_req_zone $binary_remote_addr zone=saletse_api:10m rate=30r/s;
